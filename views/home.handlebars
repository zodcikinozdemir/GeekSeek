{{#section 'head'}}
<link rel="stylesheet" href="/styles/home.css">
<link href="https://fonts.googleapis.com/css?family=Questrial" rel="stylesheet">

{{/section}}

<nav id="mainNav" class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand page-scroll" href="/">GeekSeek</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <form class="navbar-form navbar-left form-signin" method = "POST" action="/login">
                        <div class="form-group">
                           {{#if errorMessage}}
                            <div class="alert alert-danger">
                              {{errorMessage}}
                            </div>
                          {{/if}}
                          <input type="text" name="username" class="form-control" id="inputUsername SignInEmail" aria-describedby="emailSignIn" placeholder="Username">
                          <input type="password" name="password" class="form-control" id="signInPassword" aria-describedby="passwordSignIn" placeholder="Password">
                        </div>
                        <button class="btn btn-outline page-scroll btn btn-primary" id="login" type = "submit">Login</button>
                    </form>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>


    <div class = "container-fluid">
<div class="row">

{{!-- ///ABOUT//
 --}}     
  <div class="col-md-6">
	<div class = 'container'>
     </div>
  </div>
     

{{!-- //SIGN UP --}}
     <div class="col-md-6">
            <form method="POST" action="/signup" class="form-signin">
      <h2 class="form-signin-heading">Looking for a Geek?</h2>

      {{#if errorMessage}}
        <div class="alert alert-danger">
          {{errorMessage}}
        </div>
      {{/if}}
      {{!-- <input type="radio" name="usertype" value="seeker" checked> Seeker</input>
      <input type="radio" name="usertype" value="geek">Geek</input> --}}
	<div class = 'form-group'>
      <label for="inputUsername" class="sr-only">Username</label>
      <input type="text" id="inputUsername" name="username" class="form-control" placeholder="Username" required autofocus>
      </div>
      <div class = 'form-group'>
      <label for="inputPassword" class="sr-only">Password</label>
      <input type="password" id="inputPassword" name="password" class="form-control" placeholder="Password" required>
   </div>
   <div class = 'form-group' >  

      <label for="inputPassword" class="sr-only">Repeat Password</label>
      <input type="password" id="inputPassword2" name="password2" class="form-control" placeholder="Repeat Password" required>
     </div>
     <div class = 'form-group'>
		<label for="inputzipCode" name = "zipCode" class="sr-only">Zip Code</label>
  		<input type="zipCode" id="inputZipCode" name="zipCode" class="form-control" placeholder="ex. 08854" required>
  	</div>
     
      <button class="btn btn-lg btn-warning btn-block" type="submit">Sign up</button>
    </form>
    <br>
    <br>
        <div class="table-responsive">
          <h2 class="sub-header">HOW IT WORKS</h2>

          <div class="table-responsive">
  
          <p>If you're a Geek, set your skills and wait for the contact to pour in!<br>
          If you're a Seeker, run a query and find yourself a Geek!
          <h3>AND IT GETS BETTER--You can be BOTH!</h3>
          
         </p>
      </div>
    

    </div>
  </div>
  </div>